# ============================================================================
# 平台专属 UI 元数据（Codex / OpenAI 平台专用）
# ----------------------------------------------------------------------------
# 本文件只被 Codex 类运行时的“代理配置文件”加载，用于展示技能名称、简介与默认
# 提示词。WorkBuddy 等其他运行时不会读取它，也不会因此报错——它属于可选的
# 平台耦合元数据，而非技能运行所必需。若要在其他平台复用本技能，保留本文件即可，
# 它不会影响 SKILL.md 定义的通用识别 / 选色流程。
# ============================================================================
display_name: 中国风颜色参考
short_description: 从色卡图片识别中文颜色名并提供 HEX/RGB 中国风配色参考
default_prompt: 请从图片色卡中逐行识别颜色名、HEX 和 RGB，并结合中国风审美给出可用色板。
